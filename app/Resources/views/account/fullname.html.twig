

        <div class="row">
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 ">

            </div>
            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 ">
                <div class="eservice-content-inner-wrapper">
                    <div class="titleForm_main_missingcard">
                        {{ 'Dear customer'|trans}}
                    </div>
                    <div class="titleForm_sub_missingcard">
                        {{ 'You can update your information through the following form'|trans }}
                    </div>

                    {% if message != '' %}
                    <div class="alert-success-forms formMessage {{ errorcl }} alert fade in alert-dismissable">
                            {{ message }}
                    </div>
                    {% endif %}

                    <div id="preLoadersFull"></div>

                        {{ form(form) }}
                            {{ form_row(form.fullname) }}
                            {{ form_row(form.fullname_registered_iqamaid) }}
                            {{ form_row(form.comment_fullname) }}
                        {{ form_end(form) }}
                </div>
            </div>
        </div>


{% block javascripts %}
    <script type="text/javascript">
        $(document).ready(function () {
            {{ form_jquery_validation(form) }}


                    $('form[name="update_fullname"]').on('submit', function (event) {

                        if($( "#update_fullname_fullname" ).hasClass( "valid" )) {
                            if($( "#update_fullname_comment_fullname" ).hasClass( "valid" )) {

                                event.preventDefault();
                                $.ajax({
                                    type: $(this).attr('method'),
                                    url: "{{ path('front_account_fullname', {'_country': app.request.get('_country'), '_locale':app.request.get('_locale') }) }}",
                                    data: new FormData(this),
                                    processData: false,
                                    contentType: false,
                                    beforeSend: function () {
                                        $('#preLoadersFull').html('<div id="loader"></div>');
                                    },
                                    complete: function () {
                                        //$('#preLoadersFull').html('');
                                    },
                                    success: function (response) {
                                        $('#contents-8').html(response);
                                    }
                                })
                            }
                            else{
                                return false;
                            }
                        }
                        else{
                            return false;
                        }
                    });

        });

    </script>
{% endblock %}
<style>#preLoadersFull {  border-radius: 12px;  left: 40%;  position: absolute;  top: 40%;  z-index: 1;  }</style>

