{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12 text-center ">
            Activatoin step Three Enter sms
        </div>
        {{ dump(error) }}
        {% if(error.success == false) %}
            <div class="col-lg-12 text-center alert alert-danger">
                {{ error.message }}
            </div>
        {% endif %}
        <div class="col-lg-12 text-center ">

           {{ "Please enter the code you recieved via SMS"|trans }}

            {{ form_start(form) }}
            {{ form_end(form) }}

        </div>
    </div>
{% endblock %}